# 💡 思緒收集

> 日記裡任何標記 `#idea` 的項目都會自動彙整在這裡，不分生活或工作。想到什麼值得記住的念頭，就在當天日記寫 `- [ ] #idea 內容`；處理完（寫成筆記、變成計畫，或決定不做）再打勾即可。

## 💭 尚未處理
```dataview
TASK
FROM ""
WHERE !completed AND contains(tags, "#idea")
SORT start DESC
```

## ✅ 已處理（近 30 天）
```dataview
TASK
FROM ""
WHERE completed AND contains(tags, "#idea")
SORT completion DESC
LIMIT 20
```
